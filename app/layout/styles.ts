import { StyleSheet } from 'react-native';

export const gStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },

  button: {
    borderWidth: 2,
    paddingVertical: 30,
    width: '100%',
    borderRadius: 10,
    alignItems: 'center',
  },

  buttonText: {
    textTransform: 'uppercase',
    fontWeight: '600',
  },
});
